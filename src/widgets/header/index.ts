// Simple header links export used by the app to render navigation.
// Add or adapt where the project consumes header navigation.
export const headerLinks = [
	{ href: "/", label: "Home" },
	{ href: "/shared-demo", label: "Shared Demo" },
];

export default headerLinks;
export { Header } from './ui/Header'